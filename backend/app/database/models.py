# Defines the database tables as Python classes (SQLAlchemy ORM models).
# Each class here maps to one table in PostgreSQL.

# TODO: PullRequest model — tracks which PRs have been reviewed
# TODO: Review model — stores the AI-generated comments for each PR
